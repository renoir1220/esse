export function batchReferenceText(title: string, batchId: string): string {
  return `批次名称：${title}\nbatchId: ${batchId}`;
}

export function imageIdReferenceText(imageId: string): string {
  return `imageId: ${imageId}`;
}
