# Standalone App

This directory reserves the future fully independent Esse application.

The current `sidecars/agent` distribution is an Agent harness with a local HTTP MCP and an Esse workbench. It is not the standalone application described here. Do not move Sidecar code into this directory or introduce a runtime dependency between the two without a reviewed architecture decision.
